# Integrations

The app integrates with Stripe, Braintree/PayPal, mail transport, TikTok events, and optional search tooling.

Integration credentials must be loaded from `.env` through Laravel config.
