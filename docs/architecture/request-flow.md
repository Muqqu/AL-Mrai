# Request Flow

Requests enter through `public/index.php`, pass Laravel middleware, resolve a route, execute a controller action, and return a response.

Blade responses compose page-level views with shared layout partials.
