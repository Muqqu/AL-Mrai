# Models

Core models include Product, Category, Coupon, Order, OrderProduct, ProductImages, Review, Analytics, and CardDetail.

Model fillable fields should match the expected checkout and admin write paths.
