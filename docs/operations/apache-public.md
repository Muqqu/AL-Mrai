# Apache Public Rules

The public `.htaccess` file routes non-file requests to Laravel's front controller.

Keep Apache rewrite rules in `public/.htaccess` when deploying to Apache-compatible hosting.
