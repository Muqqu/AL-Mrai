# Environment Variables

Production environments must define app, database, mail, payment, and analytics variables outside Git.

After changing environment values, clear cached config before testing.
