# Local Environment

Use PHP 8.1 or newer with Composer, Node.js, and a MySQL-compatible database.

Run Composer and npm installs before serving the Laravel app through Laragon, Apache, or `php artisan serve`.
