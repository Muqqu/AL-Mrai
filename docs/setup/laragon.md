# Laragon Setup

The project path is intended to work under Laragon's `www` directory.

Point the virtual host document root at the project `public` directory so Laravel routes are handled by `public/index.php`.
