# Troubleshooting

Common setup issues are missing `.env`, absent app key, database connection failures, or missing public storage links.

Start with `php artisan config:clear` after changing environment variables.
